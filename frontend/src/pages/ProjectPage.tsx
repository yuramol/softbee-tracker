import React from 'react';
import { AddNewProject, MainWrapper } from '../components';

const ProjectPage = () => {
  return (
    <MainWrapper>
      <AddNewProject />
    </MainWrapper>
  );
};

export default ProjectPage;
